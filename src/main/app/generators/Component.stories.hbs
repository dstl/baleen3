import React from 'react'
import { {{ name }} } from '.'

export default {
  title: 'Components|{{ name }}',
  component: {{ name }},
}

export const Default: React.FC = () => {
  return  <{{ name }} />
}
